{
  "name": "tabs",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:chrome",
    "build:chrome": "vite build --mode chrome && node scripts/apply-manifest.mjs chrome",
    "build:firefox": "vite build --mode firefox && node scripts/apply-manifest.mjs firefox",
    "build:all": "npm run build:chrome && npm run build:firefox",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.258",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.4"
  }
}